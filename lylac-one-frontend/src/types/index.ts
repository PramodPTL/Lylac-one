// Shared cross-app types live here. Keep feature-specific types colocated
// with their feature (e.g. `src/features/home/types.ts`) instead of
// dumping everything into this file.

export type Nullable<T> = T | null;
